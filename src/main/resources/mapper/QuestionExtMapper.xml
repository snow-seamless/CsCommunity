<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="com.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Fri Apr 29 15:07:47 GMT+08:00 2022.
        -->
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="title" jdbcType="CHAR" property="title" />
        <result column="description" jdbcType="CHAR" property="description" />
        <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
        <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
        <result column="creator" jdbcType="CHAR" property="creator" />
        <result column="comment_count" jdbcType="BIGINT" property="commentCount" />
        <result column="view_count" jdbcType="BIGINT" property="viewCount" />
        <result column="like_count" jdbcType="BIGINT" property="likeCount" />
        <result column="tag" jdbcType="CHAR" property="tag" />
        <result column="star_count" jdbcType="BIGINT" property="starCount" />
        <result column="sticky" jdbcType="INTEGER" property="sticky" />
    </resultMap>
    <update id="incView" parameterType="com.community.model.Question">
        update question
        set VIEW_COUNT = VIEW_COUNT + #{viewCount,jdbcType=BIGINT}
        where id = #{id}
    </update>

    <update id="incCommentCount" parameterType="com.community.model.Question">
        update question
        set COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=BIGINT}
        where id = #{id}
    </update>

    <update id="incLikeCount" parameterType="com.community.model.Question">
        update question
        set LIKE_COUNT = LIKE_COUNT + #{likeCount,jdbcType=BIGINT}
        where id = #{id}
    </update>

    <update id="incStarCount" parameterType="com.community.model.Question">
        update question
        set STAR_COUNT = STAR_COUNT + #{starCount,jdbcType=BIGINT}
        where id = #{id}
    </update>

    <select id="selectRelated" parameterType="com.community.model.Question" resultMap="BaseResultMap">
        select * from question
        <where>
            <if test="id != null">
                and id != #{id}
            </if>
            <if test="tag != null and tag != ''">
                and tag regexp #{tag}
            </if>
        </where>
        order by gmt_create desc limit 20
    </select>

    <select id="countBySearch" parameterType="com.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
        select count(*) from question
        <where>
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>
            <if test="tag != null and tag != ''">
                and tag regexp #{tag}
            </if>
            <if test="sort != null and sort != '' and sort == 'no'">
                and comment_count = 0
            </if>
            <if test="time != null and time != ''">
                and gmt_create > #{time}
            </if>
            <if test="1 == 1">
                and sticky = 0
            </if>
        </where>
    </select>

    <select id="selectSticky" parameterType="com.community.dto.QuestionQueryDTO"
            resultMap="BaseResultMap">
        select *
        from question
        where sticky > 0
        order by sticky desc, gmt_create desc
    </select>

    <select id="selectBySearch" parameterType="com.community.dto.QuestionQueryDTO"
            resultMap="BaseResultMap">
        select * from question
        <where>
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>
            <if test="tag != null and tag != ''">
                and tag regexp #{tag}
            </if>
            <if test="sort != null and sort != '' and sort == 'no'">
                and comment_count = 0
            </if>
            <if test="time != null and time != ''">
                and gmt_create > #{time}
            </if>
            <if test="1 == 1">
                and sticky = 0
            </if>
        </where>
        <if test="sort == null or sort == ''">
            order by gmt_create desc
        </if>
        <if test="sort != null and sort != '' and sort == 'new'">
            order by gmt_create desc
        </if>
        <if test="sort != null and sort != '' and sort == 'no'">
            order by gmt_create desc
        </if>
        <if test="sort != null and sort != '' and (sort == 'hot' || sort == 'hot7' || sort == 'hot30')">
            order by comment_count desc
        </if>
        limit #{page},#{size}
    </select>


</mapper>